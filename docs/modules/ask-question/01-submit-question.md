# 01. Submit Question

```http
POST /ask-question
Content-Type: multipart/form-data
Auth: Bearer {{accessToken}} (BROTHER, SISTER, JUMMAH)
```

## 1. Overview
Allows a registered user (`BROTHER`, `SISTER`, or `JUMMAH`) to submit a new question. The user can optionally attach an image to provide visual context. Questions are initially stored with a `pending` status.

> **Response format**: See [Standard Response Envelope](../../README.md#standard-response-envelope)

---

## 2. Business Rules (Source of Truth)

### 2.1 Authentication & Account Status
- **Protected route** — requires a valid `Bearer` token.
- **Role restriction**: Only users with the role `BROTHER`, `SISTER`, or `JUMMAH` can submit questions.
- **Status check**: The [auth middleware](../../../src/app/middlewares/auth.ts) automatically blocks access if the user status is `DELETED`, `RESTRICTED`, `SUSPENDED`, `REJECTED`, or `INACTIVE`.
- **Identity & Role**: Both `userId` and `userRole` are automatically extracted from the authenticated token and associated with the record for security and historical tracking.

### 2.2 File Handling (image)
File upload is processed by `fileHandler` **before** validation.

- **Field**: `image` (maxCount: 1).
- **Max file size**: 10 MB (default).
- **Allowed MIME types**: `image/jpeg`, `image/png`, `image/jpg`, `image/webp`.
- **Image processing**: 800px-width resize, PNG palette-compressed level 8, JPEG/WebP quality 80.

### 2.3 Input Validation (Zod — `submitQuestionZodSchema`)
| Field | Type | Required | Constraint |
| :--- | :--- | :--- | :--- |
| `question` | `string` | Yes | Trimmed; Min 1 char (no whitespace only); Max 2000 chars. |
| `image` | `string` | No | Optional; URL string (attached by `fileHandler` after successful upload). |

### 2.4 Database State & Default Values
- **`status`**: Defaults to `pending` via Mongoose schema.
- **`userId`**: Required; associated with the submitter.
- **`userRole`**: Required; stored for role-based analytics and filtering.
- **`imageUrl`**: Optional; stores the URL generated by `fileHandler`.

---

## 3. Behavioral Notes

- **Input Sanitization**: `question` is trimmed before validation. Whitespace-only values are rejected by the `.min(1)` rule after trimming.
- **Historical Tracking**: `userRole` is captured at the time of submission. If the user's role changes later, the question still reflects the role they held when asking.
- **Timestamping**: `createdAt` and `updatedAt` are automatically generated by Mongoose timestamps.

---

## 4. Request Body (Form-Data)

| Field | Type | Required | Description | Example |
| :--- | :--- | :--- | :--- | :--- |
| `question` | `string` | Yes | The text content of the question (Max 2000) | `How to perform Wudu correctly?` |
| `image` | `file` | No | Optional image attachment (JPG/PNG/WebP) | — |

---

## 5. Implementation
- **Route**: [src/app/modules/ask-question/ask-question.route.ts](../../../src/app/modules/ask-question/ask-question.route.ts) — `router.post('/', ...)`
- **Controller**: [src/app/modules/ask-question/ask-question.controller.ts](../../../src/app/modules/ask-question/ask-question.controller.ts) — `submitQuestion`
- **Service**: [src/app/modules/ask-question/ask-question.service.ts](../../../src/app/modules/ask-question/ask-question.service.ts) — `submitQuestionIntoDB`
- **Validation**: [src/app/modules/ask-question/ask-question.validation.ts](../../../src/app/modules/ask-question/ask-question.validation.ts) — `AskQuestionValidation.submitQuestionZodSchema`

**Middleware order**: `auth(BROTHER, SISTER, JUMMAH)` -> `fileHandler([{ name: 'image', maxCount: 1 }])` -> `validateRequest(AskQuestionValidation.submitQuestionZodSchema)` -> `AskQuestionController.submitQuestion`.

---

## 6. Responses

### Success (201)
```json
{
  "success": true,
  "statusCode": 201,
  "message": "Question submitted successfully",
  "data": {
    "_id": "664a1b2c3d4e5f6a7b8c9d0e",
    "userId": "664a1b2c3d4e5f6a7b8c9d0f",
    "userRole": "BROTHER",
    "question": "How to perform Wudu correctly?",
    "imageUrl": "http://localhost:5000/uploads/images/1715421000-abc123.jpg",
    "status": "pending",
    "createdAt": "2026-05-11T10:30:00.000Z",
    "updatedAt": "2026-05-11T10:30:00.000Z"
  }
}
```
